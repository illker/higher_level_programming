#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
    matrixf = []
    for n in matrix:
        matrixc = []
        for x in n
            matrixc.append(pow(x, 2))
        matrixf.append(matrixc)
    return matrixf
